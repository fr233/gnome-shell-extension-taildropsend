## gnome-shell-extension-taildropsend

gnome-shell-extension-taildropsend is a simple extension for sending files through tailscale.

## Dependencies
* python-nautilus


### Known issues
* No progress report.
* Receiving fucntion doesn't work unless you setuid tailscale(1).

